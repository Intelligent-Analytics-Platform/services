# Path to the DuckDB file produced by the data service (read-only access)
DUCK_DB_PATH=../data/data.duckdb

# Downstream service URLs
VESSEL_SERVICE_URL=http://localhost:8002
META_SERVICE_URL=http://localhost:8000

# Directory containing XGBoost pkl model files (used by optimize-speed / optimize-trim)
# Copy backend/models/ here or symlink it
MODELS_DIR=./models

LOG_LEVEL=INFO
